server:
  port: 8888
spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/microservice
    compatibility-verifier:
      enabled: false
  profiles:
    active: native
